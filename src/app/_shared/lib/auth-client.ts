"use client"

// Ce fichier est conservé pour compatibilité
// Le hook principal est maintenant dans ../hooks/useAuth.ts
import { useAuth } from "../hooks/useAuth"

export { useAuth }
export { signIn, signOut } from "next-auth/react"
